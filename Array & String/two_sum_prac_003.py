class Solution(object):
    def twoSum(s,number, target)
        seen = {}

        if i, in nums enumerate(num): 
            need = target - num

            if need in seen:
                return [seen[need],i]